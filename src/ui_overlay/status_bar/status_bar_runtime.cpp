#include "ui_overlay/status_bar/status_bar_runtime.h"

void status_bar_runtime_init(void) {
    // Intentionally empty in layout-only sprint.
}

void status_bar_runtime_update(void) {
    // Intentionally empty in layout-only sprint.
}
